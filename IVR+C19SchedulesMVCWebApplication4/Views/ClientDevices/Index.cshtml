@model IEnumerable<IVR_C19SchedulesMVCWebApplication4.Models.ClientDevice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceList)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceList)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientDeviceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientDeviceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientDeviceId })
        </td>
    </tr>
}

</table>
